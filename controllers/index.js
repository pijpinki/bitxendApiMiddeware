const GetSymbols = require('./GetSymbols');
const ApiGetHistory = require('./api/GetHistory');
const ApiGetBalance = require('./api/GetBalance');

module.exports = [
    GetSymbols,
    ApiGetHistory,
    ApiGetBalance,
];
